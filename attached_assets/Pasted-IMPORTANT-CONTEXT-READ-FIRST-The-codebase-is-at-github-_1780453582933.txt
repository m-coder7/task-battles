IMPORTANT CONTEXT — READ FIRST:
The codebase is at github.com/m-coder7/task-battles. Pull and read it fully before starting. A previous Replit agent made significant changes including: renaming the app to "Task Battles", adding Midnight/Ember themes, shared React context for Goals/Events, subtasks, star flags, Add to Calendar toggle, settings panel, floating widget, Android widgets, and Firestore REST API fallback for Electron. All of this exists in the repo. Do not rewrite or undo any of it — build on top of it.
This is a large task. Implement everything completely and in order.

PART 1 — MIGRATE FROM ELECTRON TO TAURI
Migrate the desktop app from Electron to Tauri completely. The frontend (React + Vite + TypeScript + Tailwind) stays exactly the same — only the desktop wrapper changes. Steps:

Set up Tauri in artifacts/planner/
Remove all Electron dependencies and config
Keep all existing frontend features working
Keep the Firestore CORS/Origin fixes — implement them in Tauri's tauri.conf.json allowlist and custom headers instead of Electron's session handlers
The app should build to a native Windows .exe and macOS .dmg using tauri build
Keep the BASE_PATH and PORT setup working for the build


PART 2 — MIGRATE FROM FIRESTORE TO SUPABASE (HYBRID OFFLINE/ONLINE)
Replace Firebase/Firestore with Supabase across the entire app (desktop + mobile). Requirements:

Offline first — the app must work fully without internet. All data stored locally (localStorage on desktop, AsyncStorage on mobile)
Sync when online — when internet is detected, sync local changes to Supabase automatically
Conflict resolution — last-write-wins based on timestamp
Supabase tables needed: profiles, goals, events, notes, diary_entries, rivalry_profiles, rivalry_stats, rivalry_monthly_stats
Linked accounts — a user's data syncs across all their devices (phone + desktop) through Supabase. Same account = same data everywhere. Use a simple device-local UUID as user ID that gets linked to a Supabase account
Keep the Rivalry real-time features working — use Supabase Realtime instead of Firestore listeners
Set up Supabase RLS (Row Level Security) so users can only access their own data


PART 3 — MOBILE TAB SCREENS (fix missing files)
The mobile app at artifacts/day-planner-mobile/ is missing its core tab screens. Only settings.tsx exists in app/(tabs)/. Create all missing screens using the desktop components in artifacts/planner/src/components/ as reference:

app/(tabs)/index.tsx — Today screen (based on TodayPanel.tsx)
app/(tabs)/goals.tsx — Goals screen (based on GoalsPanel.tsx)
app/(tabs)/calendar.tsx — Calendar screen (based on calendar views)
app/(tabs)/rivalry.tsx — Rivalry screen (based on RivalryPanel.tsx)

Use existing GoalsContext, EventsContext, ThemeContext, useColors. Match Ember/Midnight themes. Keep all features: subtasks, star flag, Add to Calendar, rivalry system, 1-3-5 mode, search.

PART 4 — NOTES SECTION
Add a Notes section to both desktop (new sidebar item) and mobile (new tab). Best features from top notes apps:

Rich text editing — bold, italic, headings, bullet lists, checklists (inspired by Notion's block editor)
Folders/organization — notes organized in folders, plus tags
Search — instant full-text search across all notes
Pin important notes — pinned notes appear at top
Quick capture — create a new note in one click/tap with no friction (inspired by Apple Notes)
Markdown support — write in Markdown, renders beautifully
Link notes together — type [[note name]] to link between notes (inspired by Obsidian)
Note templates — a few starter templates (meeting notes, project plan, brain dump)
Local first — notes stored locally, sync to Supabase when online
Apply Midnight/Ember themes


PART 5 — DIARY SECTION
Add a Diary section to both desktop (new sidebar item) and mobile (new tab). Best features from top diary apps:

Daily entries — one entry per day, date-stamped automatically
Mood tracking — pick a mood (great/good/okay/bad/awful) with an emoji, shown on calendar
Rich media — attach photos to entries (mobile: camera roll, desktop: file picker)
Weather auto-tag — automatically log today's weather on each entry (using a free weather API)
On This Day — show past entries from the same date in previous years
Streak counter — how many consecutive days you've written
Templates — morning reflection, evening review, gratitude list
Search — search across all diary entries
Privacy lock — PIN or biometric lock for the diary section
Export — export diary as PDF or plain text
Local first, sync to Supabase when online
Apply Midnight/Ember themes


PART 6 — NATIVE DESKTOP WIDGETS (Tauri)
Build proper native desktop widgets for both Windows and Mac:
Windows: Build widgets for the Windows 11 Widget Board using the PWA/web widget format (widgets field in web app manifest). Create 3 widgets:

Today's Tasks widget — shows today's goals with checkboxes, checkable directly from widget
Rivalry Score widget — shows your score vs rival's score with progress bar
Daily Progress widget — circular progress ring showing goals completed today

macOS: Build a native WidgetKit widget extension using Swift alongside the Tauri app. Use App Groups to share data between the Tauri app and the widget. Create the same 3 widgets as Windows:

Today's Tasks widget — shows today's goals with checkboxes
Rivalry Score widget — shows your score vs rival's score
Daily Progress widget — circular progress ring showing goals completed today

The macOS widgets should appear in the macOS Notification Center widget gallery and support small, medium, and large sizes. Use SwiftUI for the widget UI. Data flows from the Tauri app → shared App Group container → WidgetKit extension.
Both Windows and Mac widgets should respect the user's selected theme (Midnight/Ember) and update in real time when app data changes.

PART 7 — MOBILE WIDGETS (Android + iOS)
Android: Improve the existing 4 widgets in artifacts/day-planner-mobile/widgets/. Make them actually good:

Large, readable text at all widget sizes
Tap to complete tasks directly from widget
Real-time sync with app data
Proper EAS build configuration so widgets actually work when built
Both Midnight and Ember theme support

iOS: Build iOS widgets using a native Widget Extension. Create the same 4 widget types:

Today's Tasks widget
Rivalry Score widget
Daily Progress widget
Upcoming Events widget

Set up the Widget Extension properly in the Expo/EAS config. Use SwiftUI for the widget UI. Data shared between the app and widget via App Groups.

PART 8 — CALENDAR IMPROVEMENTS

Add events through Day View — clicking on a time slot in Day View should open the New Event form pre-filled with that time
Custom days repeat option — add "Custom days" to the repeat options on New Event form, letting users pick specific days of the week (Mon/Tue/Wed/Thu/Fri/Sat/Sun), matching the same functionality already in the Goals form


PART 9 — INFRASTRUCTURE FOR MORE USERS
Set up Supabase properly for scale:

Row Level Security on all tables
Proper indexes on frequently queried columns (user_id, date, created_at)
Rate limiting via Supabase Edge Functions
User account creation flow — simple email/password signup or anonymous account that can be upgraded later
Data export endpoint — users can export all their data as JSON


Implementation order: Part 1 → Part 2 → Part 3 → Part 4 → Part 5 → Part 6 → Part 7 → Part 8 → Part 9. Do not skip any part. Read the full codebase from GitHub before starting. Do not undo any work from the previous agent.
